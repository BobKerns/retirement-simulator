{
  // Inherit the project settings.
  "extends": "../tsconfig.json",
  "include": ["*.ts", "data/samples.ts"],
  "exclude": [
    "node_modules"
  ],

  "references": [
    // {"path": "../tsconfig.json"}
  ],

  "compilerOptions": {
    "allowSyntheticDefaultImports": true,
    "incremental": true,
    "tsBuildInfoFile": "../../build/.tsbuild-info-src-tests",
    // Project layout. Changes not recommended.
    "outDir": "../../build/test",
    "rootDir": ".."
  }
}

