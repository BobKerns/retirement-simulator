{
  // Inherit the project settings.
  "extends": "../tsconfig",
  "include": [
    "*.ts",
    "aggregate/*.ts",
    "calendar/*.ts",
    "display/*.ts",
    "io/*.ts",
    "model/*.ts",
    "sim/*.ts",
    "tax/*.ts",
    "data/*.json"
, "io/validate.ts", "io/input.ts", "aggregate/aggregate.ts"  ],
  "exclude": ["../node_modules", "../build", "__test__"],

  "compilerOptions": {
    "composite": true,
    "incremental": true,
    "tsBuildInfoFile": "../build/.tsbuild-info-src",
    "outDir": "../build/src",
    "rootDir": ".",
    "forceConsistentCasingInFileNames": true
  }
}
